import Api from './api';
import cardTemplate from '../templates/cardTemplate.hbs';
import refs from './refs';

const api = new Api();

// api.fetchPopular().then(data => {
//   refs.cardListEl.innerHTML = cardTemplate(data);
// });
